{
  "name": "database",
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "build": "dotenv -e ../../.env prisma generate",
    "db:push": "dotenv -e ../../.env prisma db push --skip-generate",
    "db:start": "cross-env PGPASSWORD=jarp psql -U postgres"
  },
  "dependencies": {
    "@prisma/client": "^4.15.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "dotenv-cli": "^7.2.1",
    "prisma": "^4.15.0"
  }
}
