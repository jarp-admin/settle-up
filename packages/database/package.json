{
  "name": "database",
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "build": "prisma generate",
    "db:push": "prisma db push --skip-generate"
  },
  "dependencies": {
    "@prisma/client": "latest"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "prisma": "4.14.1"
  }
}
